import { signupForAdmin } from "../auth.controller";
import { authentificationMessage } from "../../errors";

describe("API AUTHENTIFICATION SIGNUP ADMIN /api/signup/admin", () => {
	it("should do something", async () => {
		// const result = await yourFunctionToTest(someParams);
		// expect(result).toEqual(expectedResult);
	});

	it("should handle an error scenario", async () => {
		// const result = await yourFunctionToTest(someInvalidParams);
		// expect(result).toEqual(expectedError);
	});
});
